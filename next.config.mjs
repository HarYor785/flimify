/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'flimify.com', 'image.tmdb.org', 'images.unsplash.com','i.doodcdn.com','res.cloudinary.com', 'images.pexels.com']
    }
};

export default nextConfig;
