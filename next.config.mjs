/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'flimify.com', 'image.tmdb.org', 'images.unsplash.com','i.doodcdn.com']
    }
};

export default nextConfig;
